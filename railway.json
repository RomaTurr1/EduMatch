{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build --workspace @edumatch/shared && npm run prisma:generate --workspace @edumatch/api && npm run build --workspace @edumatch/api"
  },
  "deploy": {
    "startCommand": "npm run start --workspace @edumatch/api",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300
  }
}
